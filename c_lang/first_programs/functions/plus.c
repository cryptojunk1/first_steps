#include <stdio.h>
#include <unistd.h>

int		hardcore(int x, int y)	{
					int a = x + y;
		}


int		main()	{
			int ergebnis = hardcore(1 , 50);
			printf("Das ergebnis lautet %d\n", ergebnis);
			return 0;

}

